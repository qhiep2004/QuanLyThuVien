@* @model QuanLyThuVien.ViewModels.GioSachViewModel
<style>
/* Tổng quan về trang giỏ sách */
.container {
    max-width: 900px;
    margin: auto;
    padding: 20px;
}

/* Card chứa thông tin người mượn */
.borrower-info {
    background: #f8f9fa;
    border: 1px solid #ddd;
    border-radius: 8px;
    padding: 15px;
    margin-bottom: 20px;
    box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.1);
}

/* Tiêu đề của card */
.borrower-info h5 {
    font-size: 18px;
    font-weight: bold;
    color: #007bff;
    margin-bottom: 10px;
}

/* Bảng giỏ sách */
.table {
    background: white;
    border-radius: 8px;
    overflow: hidden;
}

/* Tiêu đề cột bảng */
.table thead {
    background: #007bff;
    color: white;
    font-size: 16px;
}

/* Dòng bảng */
.table tbody tr {
    transition: background 0.3s ease;
}

.table tbody tr:hover {
    background: #f1faff;
}

/* Ảnh tài liệu */
.table img {
    border-radius: 5px;
    transition: transform 0.3s ease-in-out;
}

.table img:hover {
    transform: scale(1.1);
}

/* Nút Xóa */
.btn-danger {
    background: #dc3545;
    color: white;
    padding: 6px 12px;
    border-radius: 5px;
    transition: all 0.3s ease;
}

.btn-danger:hover {
    background: #c82333;
    transform: scale(1.05);
}

/* Nút Xác Nhận Mượn */
.btn-success {
    background: #28a745;
    color: white;
    font-weight: bold;
    padding: 10px 16px;
    border-radius: 5px;
    display: block;
    text-align: center;
    margin-top: 20px;
    transition: all 0.3s ease;
}

.btn-success:hover {
    background: #218838;
    transform: scale(1.05);
}

/* Khi giỏ sách trống */
.empty-cart {
    text-align: center;
    font-size: 18px;
    color: #888;
    padding: 20px;
}
</style>

<div class="container">
    <h3 class="text-center mb-4">📚 Giỏ Sách Của Bạn</h3>

    <!-- Hiển thị thông tin người mượn -->
    <div class="borrower-info">
        <h5>👤 Thông Tin Người Mượn</h5>
        <p><strong>Mã Người Dùng:</strong> @Model.MaNguoiDung</p>
        <p><strong>Họ Tên:</strong> @Model.HoTen</p>
        <p><strong>Loại Người Dùng:</strong> @Model.LoaiNguoiDung</p>
    </div>

    @if (Model.GioSach.Any())
    {
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Ảnh</th>
                    <th>Tên Tài Liệu</th>
                    <th>Tác Giả</th>
                    <th>Số Lượng</th>
                    <th>Hành Động</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.GioSach)
                {
                    <tr>
                        <td>
                            <img src="@item.img" alt="@item.TenTaiLieu" width="50">
                        </td>
                        <td>@item.TenTaiLieu</td>
                        <td>@item.TenTacGia</td>
                        <td>@item.SoLuong</td>
                        <td>
                            <a href="@Url.Action("XoaKhoiGio", "TaiLieu", new { maTaiLieu = item.MaTaiLieu })" class="btn btn-danger">
                                Xóa
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

        <a href="@Url.Action("XacNhanMuon", "TaiLieu")" class="btn btn-success text-center">✅ Xác Nhận Mượn</a>
    }
    else
    {
        <p class="empty-cart">Giỏ sách của bạn đang trống.</p>
    }
</div> *@
